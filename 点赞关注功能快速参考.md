# 点赞、关注和评论点赞功能 - 快速参考

## 📋 接口清单

### 1️⃣ 文章点赞
| 方法 | 路径 | 说明 | 需要登录 |
|------|------|------|----------|
| POST | `/like/article/{articleId}` | 点赞文章 | ✅ |
| DELETE | `/like/article/{articleId}` | 取消点赞 | ✅ |
| GET | `/like/article/{articleId}` | 查询是否已点赞 | ❌ (未登录返回false) |

### 2️⃣ 用户关注
| 方法 | 路径 | 说明 | 需要登录 |
|------|------|------|----------|
| POST | `/follow/{userId}` | 关注用户 | ✅ |
| DELETE | `/follow/{userId}` | 取消关注 | ✅ |
| GET | `/follow/check/{userId}` | 查询是否已关注 | ❌ (未登录返回false) |
| GET | `/follow/fans/{userId}` | 获取粉丝列表 | ❌ |
| GET | `/follow/following/{userId}` | 获取关注列表 | ❌ |

### 3️⃣ 评论点赞
| 方法 | 路径 | 说明 | 需要登录 |
|------|------|------|----------|
| POST | `/like/comment/{commentId}` | 点赞评论 | ✅ |
| DELETE | `/like/comment/{commentId}` | 取消点赞 | ✅ |
| GET | `/like/comment/{commentId}` | 查询是否已点赞 | ❌ (未登录返回false) |

### 4️⃣ 用户统计
| 方法 | 路径 | 说明 | 需要登录 |
|------|------|------|----------|
| GET | `/user/getStats?userId=xxx` | 获取用户统计信息 | ❌ (不传userId需登录) |

**返回字段：**
- `viewCount`: 总浏览量
- `commentCount`: 评论数
- `followCount`: 关注数
- `fansCount`: 粉丝数
- `likeCount`: 总获赞数

---

## 🗄️ 数据库准备

执行以下SQL创建评论点赞表：

```sql
-- 文件位置: sql/comment_like.sql
CREATE TABLE IF NOT EXISTS `comment_like` (
    `user_id` BIGINT NOT NULL COMMENT '用户ID',
    `comment_id` BIGINT NOT NULL COMMENT '评论ID',
    `create_time` DATETIME DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
    PRIMARY KEY (`user_id`, `comment_id`),
    KEY `idx_comment_id` (`comment_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='评论点赞表';
```

---

## 💡 使用示例

### JavaScript (前端调用示例)

```javascript
// 1. 点赞文章
async function likeArticle(articleId) {
    const response = await fetch(`/like/article/${articleId}`, {
        method: 'POST'
    });
    const result = await response.json();
    if (result.code === 200) {
        console.log('点赞成功');
    } else if (result.code === 401) {
        alert('请先登录');
    }
}

// 2. 取消点赞文章
async function unlikeArticle(articleId) {
    const response = await fetch(`/like/article/${articleId}`, {
        method: 'DELETE'
    });
    const result = await response.json();
    if (result.code === 200) {
        console.log('取消点赞成功');
    }
}

// 3. 查询点赞状态
async function checkLikeStatus(articleId) {
    const response = await fetch(`/like/article/${articleId}`);
    const result = await response.json();
    return result.data; // true 或 false
}

// 4. 关注用户
async function followUser(userId) {
    const response = await fetch(`/follow/${userId}`, {
        method: 'POST'
    });
    const result = await response.json();
    if (result.code === 200) {
        console.log('关注成功');
    } else if (result.code === 400) {
        alert(result.msg); // 可能是"不能关注自己"或"已经关注了"
    }
}

// 5. 获取粉丝列表
async function getFansList(userId) {
    const response = await fetch(`/follow/fans/${userId}`);
    const result = await response.json();
    return result.data; // UserFollowVO 数组
}

// 6. 获取用户统计
async function getUserStats(userId) {
    const url = userId ? `/user/getStats?userId=${userId}` : '/user/getStats';
    const response = await fetch(url);
    const result = await response.json();
    return result.data;
}
```

---

## 🔍 返回数据结构

### UserFollowVO (粉丝/关注列表项)
```json
{
    "userId": 1,
    "nickname": "张三",
    "avatarUrl": "/static/upload/avatar.jpg",
    "bio": "个人简介",
    "isFollowing": true  // 当前用户是否也关注了此人
}
```

### 用户统计数据
```json
{
    "viewCount": 1234,
    "commentCount": 56,
    "followCount": 78,
    "fansCount": 90,
    "likeCount": 345
}
```

---

## ⚠️ 注意事项

1. **Session 管理**: 确保前端请求携带 Session Cookie
2. **错误处理**: 
   - 401: 未登录，需要跳转到登录页
   - 400: 业务错误（如重复点赞），显示错误消息
3. **防抖处理**: 点赞/关注按钮建议添加防抖，避免重复点击
4. **乐观更新**: 前端可以先更新UI，再发送请求，失败则回滚

---

## 🎯 核心特性

✅ **防止重复操作**: 数据库联合主键保证唯一性  
✅ **事务安全**: 点赞和计数更新在同一事务中  
✅ **计数保护**: 取消点赞时计数不会小于0  
✅ **访客友好**: 查询类接口对未登录用户返回默认值而非错误  
✅ **性能优化**: 批量查询减少数据库访问次数  

---

## 📁 相关文件清单

### 新增文件
- `pojo/CommentLike.java` - 评论点赞实体
- `mapper/CommentLikeMapper.java` - 评论点赞Mapper
- `service/ICommentLikeService.java` - 评论点赞Service接口
- `service/impl/CommentLikeServiceImpl.java` - 评论点赞Service实现
- `controller/CommentLikeController.java` - 评论点赞Controller
- `vo/UserFollowVO.java` - 用户关注VO

### 修改文件
- `service/IArticleLikeService.java` - 添加点赞方法
- `service/impl/ArticleLikeServiceImpl.java` - 实现点赞逻辑
- `controller/ArticleLikeController.java` - 实现REST接口
- `service/IUserFollowService.java` - 添加关注方法
- `service/impl/UserFollowServiceImpl.java` - 实现关注逻辑
- `controller/UserFollowController.java` - 实现REST接口
- `controller/UserController.java` - 增强getStats接口
- `mapper/ArticleMapper.java` - 添加sumLikeCountByAuthor方法
- `mapper/ArticleMapper.xml` - 添加对应SQL

### SQL文件
- `sql/comment_like.sql` - 创建评论点赞表
