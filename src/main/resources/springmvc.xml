<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:context="http://www.springframework.org/schema/context"
       xmlns:mvc="http://www.springframework.org/schema/mvc"
       xsi:schemaLocation="http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans.xsd
                           http://www.springframework.org/schema/context https://www.springframework.org/schema/context/spring-context.xsd
                           http://www.springframework.org/schema/mvc https://www.springframework.org/schema/mvc/spring-mvc.xsd">

    <!-- 1. SpringMVC扫描控制器 -->
    <context:component-scan base-package="com.vagrant.nanoblog.controller"/>

    <!-- 2. Thymeleaf模板引擎配置（保留页面渲染功能） -->
    <bean id="templateResolver"
          class="org.thymeleaf.spring5.templateresolver.SpringResourceTemplateResolver">
        <property name="prefix" value="/WEB-INF/templates/" />
        <property name="suffix" value=".html" />
        <!-- 修复THYMELEAF警告：HTML5已废弃，改用HTML -->
        <property name="templateMode" value="HTML" />
        <property name="cacheable" value="false" />
        <property name="characterEncoding" value="UTF-8" />
    </bean>

    <bean id="templateEngine" class="org.thymeleaf.spring5.SpringTemplateEngine">
        <property name="templateResolver" ref="templateResolver" />
    </bean>

    <bean class="org.thymeleaf.spring5.view.ThymeleafViewResolver">
        <property name="templateEngine" ref="templateEngine" />
        <property name="characterEncoding" value="UTF-8"/>
    </bean>

    <!-- 3. 静态资源放行 -->
    <mvc:default-servlet-handler />

    <context:property-placeholder location="classpath:jdbc.properties" ignore-unresolvable="true"/>
    <!-- 上传图片的虚拟路径映射到磁盘固定目录 -->
    <mvc:resources mapping="/uploads/**" location="file:${upload.root}"/><!--这里报错不用管，是正常的-->

    <!-- 4. Jackson配置 - 用FactoryBean替代手动new ObjectMapper -->
    <bean id="customObjectMapper"
          class="org.springframework.http.converter.json.Jackson2ObjectMapperFactoryBean">

        <!-- 自动扫描classpath中的Jackson模块（包括JavaTimeModule） -->
        <property name="findModulesViaServiceLoader" value="true"/>

        <!-- 全局时间格式（有这个就够了，不需要featuresToDisable） -->
        <property name="dateFormat">
            <bean class="java.text.SimpleDateFormat">
                <constructor-arg value="yyyy-MM-dd HH:mm:ss"/>
            </bean>
        </property>

        <!-- 忽略null字段 -->
        <property name="serializationInclusion" value="NON_NULL"/>

    </bean>

    <!-- 文件上传解析器，id 为 multipartResolver -->
    <bean id="multipartResolver"
          class="org.springframework.web.multipart.commons.CommonsMultipartResolver">
        <!-- 最大上传大小：5MB -->
        <property name="maxUploadSize" value="5242880"/>
        <!-- 默认编码 -->
        <property name="defaultEncoding" value="UTF-8"/>
    </bean>

    <!-- 5. 开启注解驱动 + 配置Jackson消息转换器 -->
    <mvc:annotation-driven>
        <mvc:message-converters register-defaults="true">
            <bean class="org.springframework.http.converter.json.MappingJackson2HttpMessageConverter">
                <!-- 引用自定义的ObjectMapper -->
                <property name="objectMapper" ref="customObjectMapper"/>
                <!-- 支持JSON媒体类型，确保@RestController返回JSON -->
                <property name="supportedMediaTypes">
                    <list>
                        <value>application/json;charset=UTF-8</value>
                        <value>text/html;charset=UTF-8</value>
                    </list>
                </property>
            </bean>
        </mvc:message-converters>
    </mvc:annotation-driven>


</beans>